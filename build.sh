echo Uploading all files to github.com/joshbav/simple-demo-app
echo
# ALl files to automatically be added
git add .
git config user.name “joshbav” 
git commit -m "scripted commit"
git push -u origin master

